// Project: test-app | API: http://localhost:3000

// Adds two numbers and returns the result
export function add(a: number, b: number): number {
  return a + b;
}

// Example usage
const result = add(3, 7); // 10
console.log(`Result: ${result}`);
